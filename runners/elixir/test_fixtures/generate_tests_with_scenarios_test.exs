defmodule SpecsRunner.GenerateTestsWithScenariosTest do
  @moduledoc false
  use ExUnit.Case, async: true

  describe "Success" do
    test "Can be parsed"
  end

  describe "Failure" do
    test "Can be parsed"
  end
end
